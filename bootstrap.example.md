Before anything else, orient yourself in this project:

1. Run `pwd` and list the repo root. Read README.md (or the closest
   equivalent) and any rules file you find — AGENTS.md, CLAUDE.md,
   CONTRIBUTING.md. Follow those rules for the whole session.
2. If a `.agentic_context/` folder exists, bootstrap it: read its AGENTS.md
   and the three INDEX.md files, load every [CORE] KNOWLEDGE doc, and follow
   the per-task memory protocol they define.
3. Check the working state: current git branch, uncommitted changes, and the
   last 5 commits — know what was in flight before touching anything.
4. Reply with a short brief: what this project is, the stack, the working
   state, the rules you'll follow — and one question if anything critical is
   unclear. Then wait for my task.

Read-only until then: change no file during bootstrap.
